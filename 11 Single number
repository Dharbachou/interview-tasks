/*
 const arr = [1, 2, 3, 1, 2] - [3]
const arr = [4, 4, 3] - [3]
*/

const arr = [4, 4, 3];

const findNum = (num) => {
  let result = 0;

  for (let i = 0; i < num.length; i++) {
    result ^= num[i];
  }

return result; 
}

findNum(arr)
