class Calculator { 
  add(x, y) { return x + y; } 
  add(x, y, z) { return x + y + z; } 
} 
const calc = new Calculator(); 
console.log(calc.add(2, 3)); // Output: NaN (The add method with two parameters is overridden) console.log(calc.add(2, 3, 4)); // Output: 9

